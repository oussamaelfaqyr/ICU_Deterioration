# ── Streamlit Cloud runtime dependencies ──────────────────────────────────────
# Only packages needed to RUN the dashboard.
# Heavy training deps (optuna, shap, mlflow, etc.) are NOT required here.

streamlit>=1.34.0
plotly>=5.22.0
pandas>=2.0.0
numpy>=1.26.0
pyarrow>=16.0.0

# Model inference (joblib loads whichever model won training)
scikit-learn>=1.4.0
lightgbm>=4.3.0
xgboost>=2.0.0
joblib>=1.3.0
shap>=0.45.0
